# DAY 1: 基础入门——概念名词

## 一、域名
1.  **概念**
    相当于IP地址的名字（IP地址不便记忆）。可通过**域名名称系统（DNS，Domain Name System）**将域名和IP相互映射起来。

2.  **注册**
    在第三方平台注册，例如：
    *   万网（阿里云）国内
    *   GoDaddy

3.  **二级域名/多级域名**
    **例如**：`www.baidu.com`
    *   `.com` 为一级域名（顶级域名）
    *   `baidu` 为二级域名
    *   `www` 为三级域名

4.  **域名对安全测试的意义**
    *   **① 相同二级域名的网站架构、源码、密码可能相似** → 可进行**旁站攻击**。
    *   **② 两个网站域名解析后的IP地址可能相同** → 可从**旁站进入主站**。

## 二、DNS
1.  **概念**
    **域名系统（服务）协议（Domain Name System）**，用于域名和IP地址相互转换。DNS服务器可手动设置。

2.  **本地 Host 文件与 DNS 的关系**
    *   **Host 文件路径**：`C:\Windows\System32\drivers\etc\hosts`
    *   **作用**：在查找域名解析时，先查看本地 Host 文件是否有缓存，没有则查找域名服务器。（**可被用于钓鱼攻击**）

3.  **CDN 和 DNS 的关系**
    *   **CDN**：内容分发网络/节点技术（Content Delivery Network），将网站内容缓存到全球各地的节点，就近提供给用户，解决访问过慢问题。
    *   **关系**：DNS 是引导用户访问 CDN 的关键。当用户访问一个使用了 CDN 的网站时，DNS 系统会将用户的域名解析到**最合适的 CDN 节点的 IP 地址**，而不是源服务器的 IP 地址。

| 步骤 | 说明 |
| :--- | :--- |
| 1 | 用户输入域名访问网站 |
| 2 | DNS 解析域名，返回最接近用户的 CDN 节点 IP |
| 3 | 用户直接访问 CDN 节点获取缓存内容 |

4.  **常见 DNS 攻击**
| 攻击类型 | 说明 |
| :--- | :--- |
| **DNS 劫持** | 攻击服务器（地区），导致 IP 解析不正常 |
| **DNS 跳转** | 诱导用户跳转至钓鱼网站 |

## 三、常见脚本语言
*   **PHP**：小型网站开发
*   **Java-MVC**：大型网站开发
*   **Python**：搭配 Django、Flask 框架
*   **其他**：ASP、ASPX、JSP 等

## 四、后门技术特点
1.  下次更方便进入。
2.  获取到相关权限时，相当于一个“管道”，提供攻击路径。
3.  具有多种玩法、免杀特性。

## 五、Web 架构与安全漏洞
### 1. 架构模型
| 层级 | 包含内容 |
| :--- | :--- |
| **操作系统** | Windows、Linux |
| **中间件（搭建平台）** | Apache、IIS、Tomcat、Nginx |
| **数据库** | MySQL、Access、DB2、Oracle、SQL Server 等 |
| **网站源码** | 分脚本类型、分应用方向覆盖 |

### 2. Web 相关安全漏洞
*   SQL 注入
*   文件上传
*   XSS 跨站脚本
*   代码执行等

#### 漏洞分类
1.  **Web 源码漏洞**
2.  **Web 中间件漏洞**
3.  **Web 数据库漏洞**
4.  **Web 操作系统对应漏洞**
5.  **其他第三方对应漏洞** —— 如 QQ、WPS
6.  **App 或 PC 应用结合漏洞**

未完待续......